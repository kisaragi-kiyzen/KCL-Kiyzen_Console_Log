# KCL-Kiyzen_Console_Log
A boring web development project.

# One line command for isntall && clone repo && run script.
```bash
pkg update -y && pkg upgrade -y && pkg install -y git python cloudflared && git clone https://github.com/kisaragi-kiyzen/KCL-Kiyzen_Console_Log && cd REPOSITORY && pip install --upgrade pip flask && (python app.py &) && sleep 5 && cloudflared tunnel --url http://127.0.0.1:5000
```

# How to use the script
```bash
Still under development!
```
